export const LOGGED_OUT_NAV_ITEMS = ["Home", "Query"] as const;

export const LOGGED_IN_NAV_ITEMS = [
  "Home",
  "Data Upload",
  "Pod Browser",
  "Query",
  "Data Privacy",
] as const;

export const PUBLIC_ROUTE_NAMES = ["Home", "Login Page", "Query"] as const;
